import { questions } from "../data/questions.js";
import { sprite, score, hpBar, footer } from "./common.js";
export function resultScreen(game) {
  const win = game.result === "victory";
  const hits = game.answers.filter((a) => a.correct).length;
  return `<section class="result-screen page-shell ${win ? "is-victory" : "is-defeat"}"><div class="screen-topline"><a class="text-button" href="#/menu">← VOLTAR AO MENU</a><span class="eyebrow">SAVE ${game.slot} / QUEST COMPLETA</span></div><div class="result-layout"><div class="result-copy"><p class="eyebrow">${win ? "YOU WIN! / FLUXO DOMINADO" : "FIM DE JOGO. COMEÇO DO APRENDIZADO."}</p><h1 tabindex="-1">${win ? "SCRUMBAN<br /><span>MASTER!</span>" : "GAME<br /><span>OVER.</span>"}</h1><p class="result-description">${win ? "Você dominou o fluxo e venceu essa batalha.<br />O próximo nível começa com o que você aprendeu." : "Até os melhores precisam de mais uma tentativa.<br />Revise suas jogadas e volte para a revanche."}</p><div class="result-stats"><div><strong>${hits}<small>/5</small></strong><span>ACERTOS</span></div><div><strong>${questions.length - hits}</strong><span>ERROS</span></div><div><strong>${score(game.score)}</strong><span>PONTOS</span></div></div><div class="result-actions"><button class="game-button" data-action="new" data-slot="${game.slot}">${win ? "JOGAR NOVAMENTE" : "TENTAR NOVAMENTE"} <span>↗</span></button><a class="text-button" href="#/menu">VOLTAR AO MENU →</a></div></div><div class="result-art" aria-label="${win ? "Mario comemora sua vitória" : "Bowser vence esta batalha"}"><span class="result-stamp">${win ? "QUEST<br />CLEARED" : "TRY<br />AGAIN"}</span>${sprite(win ? "mario-win" : "bowser-win", "result-main-character")}${sprite(win ? "star" : "mario-hurt", "result-side-character")}<div class="result-hp">${hpBar("MARIO", game.marioHP, "player")}${hpBar("BOWSER", game.bowserHP, "enemy")}</div></div></div><details class="answer-review"><summary>REVISAR MINHAS JOGADAS <span>+</span></summary><ol>${game.answers.map((a, i) => `<li><span class="review-mark ${a.correct ? "correct" : "wrong"}">${a.correct ? "ACERTO" : "ERRO"}</span><div><h3>${questions[i].topic}</h3><p>Sua resposta: ${questions[i].options[a.choice]}</p>${a.correct ? "" : `<p><strong>Resposta certa:</strong> ${questions[i].options[questions[i].answer]}</p>`}<p>${questions[i].explanation}</p></div></li>`).join("")}</ol></details>${footer()}</section>`;
}
